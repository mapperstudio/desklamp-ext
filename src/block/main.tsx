import React from 'react';
import ReactDOM from 'react-dom/client';
import BlockScreen from './App';
import '../styles/tailwind.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BlockScreen />
  </React.StrictMode>
);
